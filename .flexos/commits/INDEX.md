# commits/

Sequential changelog entries. One file per commit.

**Naming:** `NNN-{slug}.md` (zero-padded, monotonic)

Every spec change, every design decision, every skill run writes a commit. The project's memory of how it evolved.
